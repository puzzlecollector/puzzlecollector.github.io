/*
Write a short program that retrieves and prints the 
type of OS you are using by the "platform" method from the 
"os" module.  
*/ 
const os = require("os"); 
console.log(os.platform()); // darwin is printed  